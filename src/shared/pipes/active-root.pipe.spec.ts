import { ActiveRootPipe } from './active-root.pipe';

describe('ActiveRootPipe', () => {
  it('create an instance', () => {
    const pipe = new ActiveRootPipe();
    expect(pipe).toBeTruthy();
  });
});
